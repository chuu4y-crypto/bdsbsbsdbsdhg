-- Instantly wipe any old loops running in the background
_G.DesyncActive = false
task.wait(0.03)

_G.DesyncActive = true

task.spawn(function()
    local settings = settings():GetService("NetworkSettings")
    
    -- High-power oscillation loop to break server synchronization
    while _G.DesyncActive do
        -- Completely blocks your outgoing updates from reaching other players
        settings.InboundReplicationLag = 999999
        task.wait(0.38) -- Hold the freeze long enough to make you teleport huge distances
        
        -- Forces a sudden, intense packet burst to update your true position instantly
        settings.InboundReplicationLag = 0
        task.wait(0.01)
    end
end)
