-- Breaks the active loop condition
_G.DesyncActive = false

-- Reset physics boundaries back to normal default values
local Player = game:GetService("Players").LocalPlayer
if Player then
    Player.SimulationRadius = 100
    Player.MaxSimulationRadius = 100
end
