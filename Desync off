-- Forces the background thread to break out of the loop instantly
_G.InsaneLagActive = false

-- Immediate, absolute reset of the replication lag buffer
pcall(function()
    local netSettings = settings():GetService("NetworkSettings")
    if netSettings then
        netSettings.InboundReplicationLag = 0
    end
end)
