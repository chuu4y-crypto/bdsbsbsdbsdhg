-- Break the lag loop condition immediately
_G.ExtremeLagswitchActive = false

-- Restore real-time bandwidth and latency settings safely
pcall(function()
    local netSettings = settings():GetService("NetworkSettings")
    if netSettings then
        netSettings.InboundReplicationLag = 0
    end
end)
