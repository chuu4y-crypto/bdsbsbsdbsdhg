-- Instantly signals the loop thread to terminate
_G.DesyncActive = false

-- Protected call to reset network settings safely without throwing errors
pcall(function()
    local netSettings = settings():GetService("NetworkSettings")
    if netSettings then
        netSettings.InboundReplicationLag = 0
    end
end)
